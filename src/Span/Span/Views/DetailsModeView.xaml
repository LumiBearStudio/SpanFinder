<UserControl
    x:Class="Span.Views.DetailsModeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Span.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- DataGrid Row Style -->
        <Style x:Key="CustomDataGridRowStyle" TargetType="controls:DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="MinHeight" Value="18"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="CustomDataGridCellStyle" TargetType="controls:DataGridCell">
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:DataGridCell">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                Margin="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        <controls:DataGrid
            x:Name="DetailsDataGrid"
            ItemsSource="{Binding CurrentItems, Mode=OneWay}"
            SelectedItem="{Binding CurrentFolder.SelectedChild, Mode=TwoWay}"
            SelectionMode="Extended"
            AutoGenerateColumns="False"
            CanUserReorderColumns="True"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            GridLinesVisibility="None"
            HeadersVisibility="Column"
            RowHeight="18"
            ColumnHeaderHeight="20"
            FontSize="11"
            IsReadOnly="True"
            Background="Transparent"
            RowBackground="#0A000000"
            RowStyle="{StaticResource CustomDataGridRowStyle}"
            CellStyle="{StaticResource CustomDataGridCellStyle}"
            AlternatingRowBackground="#12000000"
            AlternatingRowForeground="{ThemeResource TextFillColorPrimaryBrush}"
            Sorting="OnDataGridSorting"
            ColumnReordered="OnColumnReordered"
            DoubleTapped="OnItemDoubleClick"
            KeyDown="OnDetailsKeyDown">

            <controls:DataGrid.Columns>
                <!-- Icon Column -->
                <controls:DataGridTemplateColumn
                    Header=""
                    Width="40"
                    CanUserReorder="False"
                    CanUserResize="False"
                    CanUserSort="False"
                    Tag="Icon">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="vm:FileSystemViewModel">
                            <FontIcon FontFamily="{StaticResource RemixIcons}"
                                      Glyph="{x:Bind IconGlyph, Mode=OneWay}"
                                      Foreground="{x:Bind IconBrush, Mode=OneWay}"
                                      FontSize="16"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <!-- Name Column -->
                <controls:DataGridTextColumn
                    Header="Name"
                    Binding="{Binding Name}"
                    Width="*"
                    MinWidth="180"
                    CanUserSort="True"
                    Tag="Name"/>

                <!-- Date Modified Column -->
                <controls:DataGridTextColumn
                    Header="Date Modified"
                    Binding="{Binding DateModified}"
                    Width="200"
                    MinWidth="140"
                    CanUserSort="True"
                    Tag="DateModified"/>

                <!-- Type Column -->
                <controls:DataGridTextColumn
                    Header="Type"
                    Binding="{Binding FileType}"
                    Width="150"
                    MinWidth="90"
                    CanUserSort="True"
                    Tag="Type"/>

                <!-- Size Column -->
                <controls:DataGridTextColumn
                    Header="Size"
                    Binding="{Binding Size}"
                    Width="100"
                    MinWidth="80"
                    CanUserSort="True"
                    Tag="Size"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</UserControl>
